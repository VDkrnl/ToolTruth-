const api=window.TOOLTRUTH_CONFIG.API_BASE_URL;document.getElementById("run").onclick=async()=>{const btn=document.getElementById("run");btn.disabled=true;btn.textContent="Running…";try{const r=await fetch(`${api}/benchmark/run`,{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${sessionStorage.getItem("tooltruth_admin_token")||""}`},body:JSON.stringify({domain:document.getElementById("domain").value,mode:document.getElementById("mode").value})});const d=await r.json();document.getElementById("metrics").innerHTML=[["Success",`${(d.task_success_rate*100).toFixed(1)}%`],["Precision",d.precision],["Recall",d.recall],["F1",d.f1],["Unsafe prevented",d.unsafe_actions_prevented],["Latency",`${d.avg_latency_ms} ms`]].map(x=>`<article class="feature-card glass"><span class="tag">${x[0]}</span><h3>${x[1]}</h3></article>`).join("");document.getElementById("rows").innerHTML=`<table style="width:100%"><thead><tr><th>Task</th><th>Expected</th><th>Outcome</th><th>Correct</th><th>Fault</th></tr></thead><tbody>${d.results.map(x=>`<tr><td>${x.task_id}</td><td>${x.expected}</td><td>${x.outcome}</td><td>${x.correct?"✓":"✕"}</td><td>${x.fault_type||"—"}</td></tr>`).join("")}</tbody></table>`}catch(e){document.getElementById("metrics").innerHTML='<article class="feature-card glass"><h3>Backend unavailable</h3><p>Start FastAPI on port 8000 and try again.</p></article>'}finally{btn.disabled=false;btn.textContent="Run 25 tasks"}};